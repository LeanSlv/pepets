@model List<PePets.Models.Advert>

<div class="row justify-content-center" id="posts">
    @foreach (Advert advert in Model)
    {
        <div class="col-sm-10">
            @await Html.PartialAsync("~/Views/Advert/AdvertCard.cshtml", advert)
        </div>
    }
</div>
