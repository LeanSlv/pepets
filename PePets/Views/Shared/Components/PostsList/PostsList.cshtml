@model List<PePets.Models.Post>

<div class="row justify-content-center" id="posts">
    @foreach (Post post in Model)
    {
        <div class="col-sm-10">
            @await Html.PartialAsync("~/Views/Posts/PostCard.cshtml", post)
        </div>
    }
</div>
